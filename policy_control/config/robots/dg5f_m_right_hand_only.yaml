# right 손(DG-5F-M) **단독** — 팔 지령 경로 없이 손만 pd 로 제어한다.
# ★2026-09-07 사용자 지시: 손 PD 제어를 완성하는 동안 팔은 필요 없으니 팔 제어를 끄고 진행한다.
#   `groups` 에서 right_arm 만 뺀다. `sources` 는 그대로 둔다 — pd 는 q_meas 로 FSM·워치독·
#   추종오차를 계산하고, 그게 없으면 손 지령도 안 나간다.
#   손은 자기 controller_manager(Modbus TCP)라 컨트롤러 교대(switch)가 없다.
# 손은 자기 namespace(dg5f_right)의 controller_manager 에서 돌아 손 관절은 전역 /joint_states 에 나오지 않는다.
# 손 관절 순서는 드라이버/프로필 순(finger-major). 한 팔씩 시험한다(플랜 §7).
robot: dg5f_m_right_real
joint_profiles:                          # 본 프로필(양팔 + 양손). 좌손 보충은 09.14 upstream 에 들어와 삭제
  - robot_control/src/robot_control/profiles/openarm_tesollo.yaml
sources:
  arm:
    type: joint_state
    topic: /joint_states
    joints: [r_aj_1, r_aj_2, r_aj_3, r_aj_4, r_aj_5, r_aj_6, r_aj_7]
    naming: source
    stale_sec: 0.5
  ee:
    type: joint_state
    topic: /dg5f_right/joint_states
    joints: [r_hj_thumb_1, r_hj_thumb_2, r_hj_thumb_3, r_hj_thumb_4,
             r_hj_index_1, r_hj_index_2, r_hj_index_3, r_hj_index_4,
             r_hj_middle_1, r_hj_middle_2, r_hj_middle_3, r_hj_middle_4,
             r_hj_ring_1, r_hj_ring_2, r_hj_ring_3, r_hj_ring_4,
             r_hj_pinky_1, r_hj_pinky_2, r_hj_pinky_3, r_hj_pinky_4]
    naming: source
    effort_unit: raw_current              # DG-5F effort 필드는 N·m 가 아니라 raw 전류
    stale_sec: 0.5
  tip_force:
    type: float_array
    topic: /dg5f_right/tip_forces_xyz          # tip_contact_pub: 15D tip-major, 팁 로컬 프레임 [N]
    tips: [r_hl_thumb_tip, r_hl_index_tip, r_hl_middle_tip, r_hl_ring_tip, r_hl_pinky_tip]
    axes: 3
    stale_sec: 0.5
  object:
    type: pose
    topic: /objects/cup_big_s100/pose
    frame: base_link
    mode: live
    stale_sec: 0.5
  decoder_target:
    type: joint_state
    topic: /policy_control/joint_target   # 직전 시너지 손 목표 (canonical) → joint_err
    joints: [r_hj_thumb_1, r_hj_thumb_2, r_hj_thumb_3, r_hj_thumb_4,
             r_hj_index_1, r_hj_index_2, r_hj_index_3, r_hj_index_4,
             r_hj_middle_1, r_hj_middle_2, r_hj_middle_3, r_hj_middle_4,
             r_hj_ring_1, r_hj_ring_2, r_hj_ring_3, r_hj_ring_4,
             r_hj_pinky_1, r_hj_pinky_2, r_hj_pinky_3, r_hj_pinky_4]
    naming: canonical
    required: false
    stale_sec: 1.0
  head:
    type: joint_state
    topic: /head/joint_states
    joints: [head_j_pan, head_j_tilt]
    naming: canonical
    required: false
    stale_sec: 1.0
groups:
  right_hand:
    backend: dg5f_jtc
    side: right                                  # ★손만 있는 구성에서도 팔을 식별할 수 있게(RobotCfg.sides)
    topic: /dg5f_right/dg5f_right_controller/joint_trajectory
    namespace: dg5f_right                        # 드라이버 파라미터(PID p 적용) 대상
    pid_p: 1.5   # 2026-09-06 사용자 확정: DG-5F 손도 벤더 기본값만(구 실기 튜닝 4.5 폐기)
table:
  top: 0.205
  clearance_min: -0.01   # sim(테이블 0.200)에서 손가락이 판에 닿는 파지 행동(−5.8 mm) 허용 — 실기는 재학습 후 확정
  center_xy: [0.375, -0.165]
  size_xy: [0.550, 0.970]
